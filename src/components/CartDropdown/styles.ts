import styled from "styled-components";
import { Box } from "components";

export const CartDropdownStyled = styled(Box)`
  .image {
    max-width: 50px;

    img {
      max-height: 100%;
      object-fit: contain;
      width: 100%;
    }
  }
`;
